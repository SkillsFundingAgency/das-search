@model Sfa.Eds.Das.Web.ViewModels.ProviderSearchResultViewModel

<div>

    @if (Model.HasError)
    {
        @RenderErrorMessage()
    }
    else if (Model.TotalResults == 0)
    {
        @RenderZeroResult();
    }
    else if (Model.TotalResults == 1)
    {
        @RenderMessageOneResult()
    }
    else
    {
        @RenderMessage()
    }
</div>

@helper  RenderErrorMessage()
{
    <h2 class="heading-large">
        Training standards
    </h2>
    <p>
        There was a problem performing a search. Try again later.
    </p>
}

@helper RenderZeroResult()
{
    <h2 class="heading-large">
        Training providers
    </h2>
    <p>
        There are currently no providers for the apprenticeship standard: '@Model.StandardName' in '@Model.PostCode'
    </p>
}

@helper  RenderMessageOneResult()
{
    <h2 class="heading-large">
        Training providers
    </h2>
    <p>
        There is @Model.TotalResults training provider for the apprenticeship standard: '@Model.StandardName'.
    </p>
    <p>
        Results are ordered by distance from '@Model.PostCode'
    </p>
}


@helper  RenderMessage()
{
    <h2 class="heading-large">
        Training providers
    </h2>
    <p>
        There are @Model.TotalResults training providers for the apprenticeship standard: '@Model.StandardName'.
    </p>
    <p>
        Results are ordered by distance from '@Model.PostCode'
    </p>
}
