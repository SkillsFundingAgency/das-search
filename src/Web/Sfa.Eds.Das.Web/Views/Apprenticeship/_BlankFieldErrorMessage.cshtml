@model Sfa.Eds.Das.Web.ViewModels.IHasError

<div>
        @if (Model.HasError)
        {
            @RenderErrorMessage()
        }
</div>

    @helper  RenderErrorMessage()
{
    <p id="postcode-error">
        This field can't be blank
    </p>
}

